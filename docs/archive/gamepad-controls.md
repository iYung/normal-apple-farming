# Gamepad Controls Checklist

Reference design: `docs/design/gamepad-controls.md`

---

- [x] Task A — `core/lua/input.lua` — Add gamepad support to the Input class. Add `_mode = "keyboard"` and `_joystick = nil` fields in `Input.new`. In `update()`, after the keyboard loop, poll the joystick if connected: read left-stick axes (threshold ±0.3) plus D-pad for movement, A→interact, Y→pickup, B→cancel; set any true action in `new_down`, then auto-switch `_mode` to `"gamepad"` if any gamepad input is detected. Add `key_for(action)` method: returns the gamepad symbol from a NAD-specific `_PAD_LABELS` table when `_mode == "gamepad"`, otherwise returns `self._map[action] and self._map[action][1]`. Add `icon_key_for(action)` method: returns asset key from `_PAD_ICON_KEYS` in gamepad mode, nil otherwise. NAF-specific label/icon tables: move_up=`↑`, move_down=`↓`, move_left=`←`, move_right=`→`, interact=`[A]`/`btn_a`, pickup=`[Y]`/`btn_y`, cancel=`[B]`/`btn_b`. No other files change in this task.

- [x] Task B — `game/scenes/start_scene.lua` — Accept an optional `input` parameter in `StartScene.new(scene_manager, settings_state, input)`. Store it as `self.input`. If nil, fall back to `Input.new({ interact = { "e" } })` (preserves current behaviour). Update the draw method: change the hardcoded `"Press E to start"` to use `(self.input:key_for("interact") or "E"):upper()` so the correct key/button is shown. When creating `GameScene` on interact press, pass `self.input` as the third argument: `GameScene.new(self.scene_manager, self.settings_state, self.input)`. No other files change.

- [x] Task C — `game/scenes/game_scene.lua` — Accept an optional `input` parameter in `GameScene.new(scene_manager, settings_state, input)`. Store as `self._ext_input`. In `on_enter`, if `_ext_input` is provided use it directly as `self.input` (skip `Input.new` and the `_map` assignment); otherwise keep the existing `Input.new(...)` creation and `_map` assignment as fallback. Pass `self.input` as fourth arg to `ShopScene.new(...)` and third arg to `BookScene.new(...)`. Pass `self.input` to `ActionsInfo.new(self.input)` instead of `self._settings_state.keybinds`. If using external input, do NOT call `input:update()` from within `GameScene:update()` (main loop owns it); the existing call is through `self.player:update(dt, self)` which calls `self.input:update()` inside player — leave player's call for now (player task removes it). No other files change.

- [x] Task D — `game/scenes/shop_scene.lua` — Accept an optional `input` parameter as fourth arg to `ShopScene.new(game_state, scene_manager, game_scene, input)`. If provided, store as `self.input` and skip the internal `Input.new(...)`. If not provided, keep the existing `Input.new(...)` as fallback. Rename the action keys inside the existing internal map from `left`/`right` to `move_left`/`move_right` (so the fallback also works). Change all `self.input:pressed("left")` to `"move_left"` and `"right"` to `"move_right"`. Change `self.input:pressed("cancel")` — the cancel action now exits the shop; keep `self.input:pressed("move_down")` as an additional keyboard fallback so pressing `s`/`down` still works. Remove `self.input:update()` when external input is used (add `if not input then self.input:update() end` guard). No other files change.

- [x] Task E — `game/scenes/book_scene.lua` — Accept an optional `input` parameter as third arg to `BookScene.new(game_scene, scene_manager, input)`. If provided, store as `self.input` and skip `Input.new`. If not provided, fall back to existing `Input.new({ interact = {"e"} })`. Remove `self.input:update()` when external input is provided (add `if not input then self.input:update() end` guard). No other files change.

- [x] Task F — `game/entities/player.lua` — Remove the call to `self.input:update()` at the top of `Player:update(dt, scene)`. Main.lua will own the update cycle once the feature is fully wired. Remove the fallback `Input.new({...})` default inside `Player.new` — always expect input to be passed in as the third argument; if nil, raise an error or leave nil (no silent fallback). No other files change.

- [x] Task G — `game/scenes/settings_menu.lua` — Add a local `_joy_nav(input)` function that reads `input._joystick` axes and buttons (same pattern as wip): returns a table with `up`, `down`, `left`, `right`, `confirm` booleans. In `update()`, after computing keyboard booleans, OR in the `_joy_nav` results. Add a `gamepadpressed(button)` method: if button is `"start"`, act as escape (close menu / exit keybinds sub-screen / cancel capture). Snapshot gamepad state in `open()` alongside the keyboard snapshot to prevent ghost-fire. In `draw()` and `update()`, hide the "Keybinds" item (item index 2) when `self._input._mode == "gamepad"` — skip drawing it and skip reacting to it (adjust selected index wrap-around using a visible-items list like wip's `_visible_items`). The menu already receives `self._input` from its constructor, so no constructor change is needed. No other files change.

- [x] Task H — `game/ui/actions_info.lua` — Change `ActionsInfo.new(keybinds)` to `ActionsInfo.new(input)`. Store as `self._input`. Remove `self._keybinds`. Update the local `key_label(action)` helper to call `self._input:key_for(action)` — in keyboard mode this returns the key string, in gamepad mode it returns `[A]`/`[Y]` etc. Wrap the return in `"["..k:upper().."]"` only when in keyboard mode (detect by checking if the returned value already starts with `[`). Simplest approach: call `self._input:key_for(action)` and use the result directly as the label — key_for already wraps gamepad labels in `[...]`. For keyboard mode where key_for returns a bare key string like `"e"`, keep the existing `"[" .. k:upper() .. "]"` wrapping. Update `game_scene.lua` call site: `ActionsInfo.new(self.input)` (this is already covered by Task C but note it here for coordination). No other files change.

- [x] Task I — `main.lua` — Create a single shared Input object with the full action map. Build the initial map: `{ move_up={"w","up"}, move_down={"s","down"}, move_left={"a","left"}, move_right={"d","right"}, interact={"e"}, pickup={"f"}, cancel={"escape"} }`. Apply settings keybinds: iterate `ss:key_map()` and overwrite matching actions, but ensure `cancel={"escape"}` is preserved after the merge. Store as `local input` at module level. Pass `input` to `StartScene.new(manager, ss, input)`. In `love.update`'s else-branch, call `input:update()` before `manager:update(dt)`. Add `love.joystickadded(joystick)`: if gamepad and `input._joystick == nil`, set `input._joystick = joystick`. Add `love.joystickremoved(joystick)`: clear `input._joystick` and scan remaining joysticks for a replacement. Add `love.gamepadpressed(joystick, button)`: set `input._joystick = joystick`, `input._mode = "gamepad"`; if button is `"start"` and settings is not open, call `settings_menu:open()`; if settings is open, call `settings_menu:gamepadpressed(button)`. In `love.keypressed`: set `input._mode = "keyboard"` before the existing routing logic. When keybinds change in settings (after `set_keybind`), the settings_menu already does `self._input._map = self._state:key_map()` — add `self._input._map.cancel = {"escape"}` after that line to re-inject cancel (the settings_menu `keypressed` method needs this one-line addition too). Update `settings_menu = SettingsMenu.new(ss, input, on_close)` to pass the shared `input`. No other files change beyond what's noted here except the one-line addition to settings_menu.lua's `keypressed` (acceptable in this task since it's a one-liner and avoids a round-trip).

- [x] Task J — `tests/test_input.lua` (new file) — Write unit tests for the new Input methods. Port from wip's `tests/test_input.lua` with NAF action names. Cover: (1) `key_for` returns first keyboard key in keyboard mode; (2) `key_for` returns gamepad label in gamepad mode (↑↓←→ [A] [Y] [B]); (3) `key_for` returns nil for unknown action; (4) joystick A button drives `interact` `_down` and `_pressed` correctly across frames; (5) left-stick Y axis drives `move_up`/`move_down`; (6) `_mode` auto-switches to `"gamepad"` on first gamepad input; (7) disconnected joystick is ignored; (8) ghost-interact prevention via priming update; (9) `icon_key_for` returns nil in keyboard mode; (10) `icon_key_for` returns `btn_a`/`btn_y`/`btn_b` in gamepad mode. Use the headless `love.keyboard.isDown` stub already present; mock the joystick inline as a table with `isConnected`, `getGamepadAxis`, `isGamepadDown` methods.
