## Headless Breed Benchmark Checklist

- [x] Task A — `tests/test_breed_benchmark.lua` — Create the benchmark test file. Seed two parent animals each with `height=1`. Install a deterministic `math.random` stub (returns `0` for `(0,1)` calls and `0.0` for no-arg calls, falls through to the real `math.random` otherwise) so every `AnimalStats.breed` call increases `height` by exactly 1. Loop 999,999 times, feeding each offspring back as both parents. Wrap the loop with `os.clock()` and print `breed_benchmark: 999999 generations to height=1000000 in X.XXXs` followed by `PASS: breed benchmark completed`. Restore `math.random` to the real implementation immediately after the loop. Do not assert a time threshold. Follow the flat-script pattern of existing files in `tests/` (no `runner.setup`, no `Breeder`, no scene — call `AnimalStats.breed` directly as `test_animal_stats.lua` does).
