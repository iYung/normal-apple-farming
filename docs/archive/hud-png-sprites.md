## HUD PNG Sprites Checklist

- [x] Task A — `assets/images/hud/` — Copy 7 PNGs from `../godot-animal-game` into a new `assets/images/hud/` folder: `money_info_container.png` (from `objects/money_info/`), `info_container.png` and `color_swatch.png` (from `objects/animal_info/`), `job_info_top.png`, `job_info_mid.png`, `job_info_bottom.png`, and `check.png` (from `objects/job_info/`).

- [x] Task B — `game/ui/money_info.lua` — Load `assets/images/hud/money_info_container.png` at module top. In `draw()`, replace the two `love.graphics.rectangle` calls (fill + line) with a single `love.graphics.draw(img, x, y, 0, sx, sy)` scaled so the PNG renders at approximately 192×96. Keep money text drawn on top unchanged.

- [x] Task C — `game/ui/animal_info.lua` — Load `assets/images/hud/info_container.png` and `assets/images/hud/color_swatch.png` at module top. In `draw()`, replace the two background `rectangle` calls with `love.graphics.draw(info_container, x, y, 0, sx, sy)` scaled to fit the existing panel area (~192×96). Replace the two color-swatch `rectangle` calls (fill + line) with `love.graphics.setColor(r, g, b, 1)` then `love.graphics.draw(color_swatch, sx, sy)` to tint the swatch sprite with the animal's color. Reset color to white after.

- [x] Task D — `game/ui/job_info.lua` — Load `assets/images/hud/job_info_top.png`, `job_info_mid.png`, `job_info_bottom.png`, and `check.png` at module top. In `draw()`, for each active job replace the two `rectangle` calls with: draw `top` at `(panel_x, jy)` (PNG is 192×42), then for each goal row draw `mid` at `(panel_x, jy + 42 + row_index * 30)` (PNG is 192×30), then draw `bottom` immediately after the last mid row (PNG is 192×19). Scale all three PNGs to `panel_w / 192` horizontally. Reward text `$job.reward` moves to sit just above the bottom PNG. Remove all `rectangle` calls.

- [x] Task E — `game/ui/actions_info.lua` — Add `local ui = require("game/ui")` at the top (it exports `draw_hud_box` and `draw_currency_bubble`). In `draw()`, replace the `love.graphics.rectangle("fill", ...)` call with a call to `ui.draw_hud_box({hint_text}, font, margin)` — pass the combined hint string as a single-element table. Remove the manual `rectangle` call and the manual `print` call (since `draw_hud_box` currently only draws the box — add text drawing inside or after the box call using `love.graphics.print` at the correct `box_x + PAD, box_y + PAD` coordinates). Use the same `speech_bubble.png` and 12-px margin already configured in `ui.lua`.
